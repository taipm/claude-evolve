---
description: Show claude-evolve learning store stats for this project
---

Run the status report and show it to the user verbatim:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/status.py"
```

Then give a one-line read on the health of the loop (are learnings accumulating, any
promoted skills, anything stale that should be reviewed).
